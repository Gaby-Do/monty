#include "monty.h"
/**
 * tokenizar - parse a string to get maybe opcode
 * @str: string to parse
 * Return: pointer to array of tokens
 */
char *tokenizar(char *str)
{
	char *straux = str;

	printf("this is the tokenizacion...%s\n", straux);
	return (straux);
}
